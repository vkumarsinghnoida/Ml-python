FROM ubuntu:22.04

COPY requirements.txt .
RUN     apt update \
        apt install sudo -y \
        sudo apt install openssh-server python3 python3-pip git curl w>
        pip install -r requirements.txt 
